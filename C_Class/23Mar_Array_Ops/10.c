// WAP using array to display the name of the user in C

#include <stdio.h>

int main()
{
    char arr[40];
    printf("Enter a name: ");

    scanf("%s", arr);

    printf("You have entered: %s\n", arr);
    return 0;
}