// WAP to point a string using pointer in C.

#include <stdio.h>

int main() {
    char *str = "Hello, World!";  // Declare a pointer to a string
    
    printf("The string is: %s\n", str);  // Print the string using the pointer
    
    return 0;
}
